"""QP-CRM application package (Phase 2 stage 6b).

All Python application code lives here; the repo root keeps only the
deployment scaffolding (templates/ landing page, static/, app_data/,
app_assets/, tests/, Docker files). qp_crm.shared.config.BASE_DIR anchors
the runtime paths at the repo root, so bind mounts and URLs are unchanged.
"""
