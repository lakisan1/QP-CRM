# QP-CRM - Python requirements (pinned)
#
# Pinned via `pip freeze` from the working venv (venv/, Python 3.12) during
# Phase 0 dockerization. Regenerate only deliberately: re-running
# `venv/bin/python -m pip freeze > requirements.txt` re-pins whatever happens
# to be installed at that moment and drops this header.
#
# 'Markdown' replaces the pre-Phase-2 vendored custom_libs/markdown copy
# (same 3.10.2 version, so rendered markdown is byte-identical).

blinker==1.9.0
brotli==1.2.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cobble==0.1.4
cssselect2==0.10.1
Flask==3.1.3
fonttools==4.64.0
gunicorn==26.2.0
idna==3.19
itsdangerous==2.2.0
Jinja2==3.1.6
lxml==6.1.3
Markdown==3.10.2
mammoth==1.12.1
MarkupSafe==3.0.3
pillow==12.3.0
pycparser==3.0
pydyf==0.12.1
pypdf==6.16.2
pyphen==0.18.1
python-docx==1.2.0
requests==2.34.2
tinycss2==1.5.1
tinyhtml5==2.1.0
typing_extensions==4.16.0
urllib3==2.7.0
weasyprint==69.0
webencodings==0.6.1
Werkzeug==3.1.8
zopfli==0.4.3
