# Python stuff
__pycache__/
*.pyc
*.pyo

# Virtualenv
venv/
.env

# SQLite DB – do NOT track
app_data/pricing.db
app_data/*.db

# (optional) product images – ignore if you don't want them in git
app_data/product_images/

# OS noise
.DS_Store
Thumbs.db

# GIT 
git_notes.txt

# LOGS
pricing.log
quotation.log
*.log

# Workspace
*.code-workspace
.vscode/

# Temp
*.tmp
temp/

# Local vendored deps (install via pip / requirements.txt instead).
# NOTE: custom_libs/ is tracked since Phase 0 — it is loaded via sys.path at
# runtime, so a fresh clone + docker build needs it in the repo (Phase 2 will
# replace the vendored markdown with a pinned pip dependency).
deps/